---
import type { DataEntryMap } from "astro:content";
import { getImage } from "astro:assets";
import { ClientRouter } from "astro:transitions";
import { siteConfig } from "~/site.config";
import "~/styles/global.css";
import opengraphImageSrc from "public/opengraph.webp";
import MeasurementHeadEnd from "~/components/measurement/HeadEnd.astro";

interface Props {
  description: string;
  entry?: DataEntryMap[keyof DataEntryMap][string];
  title: string;
}

const { description, entry, title } = Astro.props;

const opengraphImage = entry?.data.image?.src ?? opengraphImageSrc;

const metadata = {
  canonical: new URL(Astro.url.pathname, Astro.site),
  description: entry?.data.description ?? description,
  image: await getImage({ src: opengraphImage, width: 1200, height: 630 }),
  title: `${entry?.data.title ?? title} ${siteConfig.title_separator} karlmantle.com`,
};
---

<head>
  <meta charset="utf-8" />
  <title data-pagefind-meta="title">{metadata.title}</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="title" content={metadata.title} />
  <meta name="description" content={metadata.description} />
  <meta name="generator" content={Astro.generator} />
  <link rel="canonical" href={metadata.canonical} />

  <meta property="og:type" content="website" />
  <meta property="og:url" content={Astro.url} />
  <meta property="og:title" content={metadata.title} />
  <meta property="og:description" content={metadata.description} />
  <meta property="og:image" content={metadata.image.src} />

  <meta property="twitter:card" content="summary_large_image" />
  <meta property="twitter:url" content={Astro.url} />
  <meta property="twitter:title" content={metadata.title} />
  <meta property="twitter:description" content={metadata.description} />
  <meta property="twitter:image" content={metadata.image.src} />

  <link
    rel="alternate"
    type="application/rss+xml"
    title=`${siteConfig.name}`
    href={new URL("rss.xml", Astro.site)}
  />

  {/* view transitions */}
  <ClientRouter />

  {/* google fonts - default noto */}
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Noto+Sans+Mono:wght@100..900&family=Noto+Sans:ital,wght@0,100..900;1,100..900&family=Noto+Serif:ital,wght@0,100..900;1,100..900&display=swap"
    rel="stylesheet"
  />

  {/* slot for extras */}
  <slot />

  <MeasurementHeadEnd />
</head>
